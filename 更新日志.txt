1.5.8 更新日志
- 改进: 将 client 这个传参删除,直接内置了,省去步骤 (但意味你的所有脚本需要将这个传参删除掉: `client *http.Client` ,详情请看示例文件 dvwaSqlScan.go)
- 改进: 添加 重定向选项: config.IsRedirect , 值为true时处理重定向后的内容
- 改进: 给 poc 中的url自动添加 http:// 或 https:// (如果没有)

更新计划:
    - 支持其他协议,不局限于http/https
    - 多写几个示例脚本,方便学会
    - 报错时不退出当前协程

